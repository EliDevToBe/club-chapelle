/** HttpOnly cookie carrying the access JWT (20-minute lifetime). */
export const CLUB_ACCESS_COOKIE = "club-access";

/** HttpOnly cookie carrying the refresh JWT (7-day lifetime). */
export const CLUB_REFRESH_COOKIE = "club-refresh";
